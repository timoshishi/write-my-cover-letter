import { PersonalData, TextResponses, CVText } from './types';
export declare const generateParagraphs: ({ textResponses, personalData, }: {
    textResponses: TextResponses;
    personalData: PersonalData;
}) => CVText;
