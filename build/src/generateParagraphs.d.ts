import { Options } from './types';
export declare const generateParagraphs: ({ industry, company, position, role, value, intro, contact, skills, personalData, }: Options) => {
    toWhomItMayConcern: string;
    introPara: string;
    roleStr: string;
    aboutMe: string;
    closer: string;
    contactInfo: string;
    name: string;
};
